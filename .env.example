# adapter-node uses this to validate request origins in production.
ORIGIN=http://localhost:3000

# Development commands run on the host and connect to the published PostgreSQL port.
DATABASE_URL=postgresql://sveltekit:sveltekit@localhost:5432/sveltekit_learning_lab
