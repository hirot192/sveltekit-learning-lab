# Replace this hostname with the public DNS name that points to the Linux server.
learn.example.com {
	encode zstd gzip
	reverse_proxy 127.0.0.1:3000

	header {
		-Server
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
	}
}
